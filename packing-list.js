const packingList = ["bowls", "plates", "pots", "eating utensils", "glasses", "cups", "cooking utensls"];

console.log("Kitchen stuff to pack:");
for (let i = 0; i < packingList.length; i++) {
  console.log(packingList[i]);
}